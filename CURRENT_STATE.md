# MyGPR 当前状态

当前版本：v0.9.24 beta

## beta 已具备

- 测线处理页已改为“连续手动处理”模式：执行当前步骤、撤回一步、重置到原始、前后对比、保存当前结果。
- 处理历史页签会显示当前叠加步骤，保存后的 processing manifest 会保留整条手动处理链。
- 正式项目创建、打开、最近项目切换和项目设置。
- 营山 / 旧 MyGPR sidecar CSV 单条导入和批量后台导入。
- 导入结果表格、坏文件诊断、raw 目录 / manifest 查看入口。
- 经纬度到 CGCS2000 / 3-degree GK 工程坐标投影。
- 项目树、测线清单、快速预览、测线处理、目标定位、空间成果和报告页的真实项目联动。
- 导入后数据质检和 B-scan 方向风险检测。
- 手动 B-scan 转置修正、修正前备份和 orientation fix manifest。
- 测线清单 CSV 导出和项目 ZIP 备份。
- 空间成果工具栏闭环：刷新空间成果、导出坐标成果、生成平面图、打开三维视图、图层控制。
- 三维成果窗口：三维轨迹、目标点、平面图、数据汇总、PNG 导出和三维点云 CSV 导出。
- 成果报告包：HTML、JSON、CSV 和 PDF 报告生成。
- 15.6 寸 1080P 笔记本 compact mode：读取可用屏幕区域并避开 Windows 任务栏。
- 已根据 Windows 真机 `1536×816` 逻辑可用区域 / `125%` 缩放截图，修复测线处理和空间成果页预览图下沉与大面积空白问题。
- 已复核用户回传的修复后截图，补充表格普通文字、选中行和处理参数面板的对比度收口。
- 已新增统一自适应布局参数系统，项目管理、测线处理、空间成果、成果报告页按窗口可用尺寸计算主图、侧栏和底部区尺寸。

## beta 不覆盖

- DZT / RD3 / DT1 / OKO 等厂商格式完整原生解码。
- Excel 成果报告生成闭环。
- PGDA-CSNet 等自动目标识别模型接入。
- 钻孔 / 开挖验证资料与空间目标点的正式关联管理。

## 当前风险

- 仍需用户本地 Windows 真实路径、真实 DPI、任务栏状态和百万行 CSV 压测。
- 取消批量导入只能在文件之间生效，不能中断正在解析的单个大文件。
- PDF 已可生成，但版式仍是 beta 报告版式；正式行业报告模板需继续细化。
- 目标识别当前是启发式辅助检测，不应宣传为深度学习模型。

## v0.9.24 当前补充状态

- 已修复项目管理页和测线处理页两个小图卡裁切风险。
- `layout_diagnostics.json` 已升级为可判定 pass/fail，并输出 `layout_check_report.json`。
- 已新增右侧辅助栏折叠机制：项目管理操作栏、测线处理参数栏、空间成果辅助栏、成果报告检查/导出栏均可收起。
- 已新增主图放大查看入口：项目/测线预览、测线处理双 B-scan、空间成果主图和辅助图可打开独立大图窗口。
- 后续布局审计优先运行 `python scripts/check_layout_diagnostics.py <capture_dir>`，再决定是否需要肉眼截图复核。

## v0.9.24 当前补充状态

- 已新增测线直接删除：测线从项目清单移除，项目目录内 raw/processed/targets/spatial 关联文件直接删除；项目目录外的原始导入来源文件不受影响。
- 已新增项目直接删除：删除当前 MyGPR 项目文件夹；项目目录外的原始导入来源文件不受影响。
- 已新增最近项目移除入口。

## v0.9.24 当前补充状态

- 已新增项目事件与 `metadata/project_state.json`，用于统一记录模块 dirty/stale 状态。
- 目标变化会标记空间成果需刷新、成果报告需重新生成。
- 空间成果刷新会清除空间 stale，并标记报告需重新生成。
- 报告生成会清除报告 stale。
- 项目树的处理结果、目标标注、空间成果和报告节点已具备导航联动。
