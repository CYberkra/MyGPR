# MyGPR v0.9.38 — Qt 图形界面使用说明

> 完整操作手册见 `docs/user/用户指南.md`（含数据格式支持矩阵、无界面批处理、备份与故障排查）。

## 环境要求
- Python 3.11 ~ 3.13
- 安装后端依赖：`pip install -r requirements-core.txt`
- 安装界面依赖：`pip install -r requirements-gui.txt`

## 启动
```bash
python app_qt.py
```
首次启动显示 600ms 开屏画面，随后进入主窗口；后端在后台初始化（右侧日志面板可见"后端初始化完成"）。

## 界面导览（左侧导航，8 页）
| 页面 | 用途 |
|---|---|
| 主页 | 当前项目概览、快速操作（新建/打开项目、导入测线）、数据预览、最近任务 |
| 项目 | 项目信息、测线列表、数据导入（支持预检）、传感器同步、处理成果(Artifact)浏览 |
| 处理 | 方法库（36 个算法，中文分类+推荐/备选/实验标签）→ 处理链编排 → 参数表单 → 运行；预设档与 AutoTune 自动调参；原始/结果 B-Scan 对比 |
| 解释 | 基覆界面标注：打开会话、点击拾取、自动追踪、吸附、平滑、撤销/重做、保存 |
| 空间 | 空间轨迹、空间成果版本、在线底图 |
| 成果 | 空间成果生成、项目报告包（PDF/HTML/Excel/ZIP）、项目备份与恢复 |
| 任务 | 任务中心：全部后台作业进度、取消、清理 |
| 设置 | 主题（浅/深）、默认介电常数、颜色映射、并行线程数、项目根目录、关于 |

右侧全局面板（可折叠，380px）：日志（级别着色终端）/ 任务（活动作业迷你视图）。

## 典型工作流
1. 主页「新建项目」→ 选择目录与项目名
2. 项目页「导入测线」→ 选择数据文件 → 预检 → 导入
3. 处理页 → 方法库双击添加算法到处理链 → 右侧调参（或直接「开始调参」AutoTune）→「运行处理链」→ 结果自动成为 Artifact 并预览
4. 解释页 → 打开标注会话 → 在 B-Scan 上点击/自动追踪 → 保存
5. 成果页 → 生成空间成果 / 报告包 / 备份

## 常见问题
- 日志文件位于 `~/MyGPR/logs/`；设置文件位于 `ui/config/settings.json` 同级的用户目录
- 后端无 Qt 依赖，可独立使用：`python backend_smoke.py --skip-autotune`、`python cli_batch.py run --config <cfg>`
- 全部数据格式（CSV/RD3/RD7/DT1/DZT/OKO/SEG-Y/ENVI/ImpulseRadar/gprMax/NPY）均可直接导入，详见用户指南第 3 节格式矩阵
