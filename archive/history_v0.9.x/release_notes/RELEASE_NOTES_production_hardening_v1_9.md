# MyGPR v0.9.28 Production Hardening V1.9

日期：2026-07-14

## 目标

在五页业务闭环和模块联动已经完成的基础上，补齐发布前的工程一致性检查、异常退出识别、大文件取消清理和可复现生产化证据。

## 已完成

- 新增项目级 `mygpr.project_integrity_report.v1`。
- 工程一致性检查覆盖：
  - 测线源文件与标准化数据；
  - 正式处理版本及登记文件；
  - 标注绑定的处理来源；
  - 空间成果引用的测线、处理、标注及成果文件；
  - 报告绑定的空间成果、正式报告目录和封印；
  - 五页工作区上下文、当前空间成果和当前报告索引；
  - 遗留 staging/tmp 项。
- 安全修复仅处理工作区指针与当前版本索引，不改写或删除正式处理、标注、空间和报告成果。
- 新增项目运行会话标记 `mygpr.project_runtime_session.v1`，识别上次会话是否正常关闭。
- 异常退出后再次打开项目时，自动运行工程引用与恢复点检查。
- 项目页“更多”和“存储与备份”中增加“工程一致性检查”。
- 页面切换和跨模块联动时同步写入运行 checkpoint。
- 修复分块复制取消后可能残留截断目标文件的问题。
- 新增 `scripts/run_release_hardening_v19.py`，生成大文件导入、取消清理、跨模块引用修复和崩溃标记证据。

## 验证

- 80 MB NPY 大测线导入演练通过，落盘为目录式 mmap 数据集。
- 分块复制取消后，目标残片和 staging 均清理。
- 工作区无效测线、处理来源、标注来源、空间成果和报告指针可安全修复。
- 异常会话识别和正常关闭标记通过。
- 五页 1672×941 离屏截图通过；布局、视觉舒适度和文本适配均为 0 问题。
- Windows 11 100%/125%/150% 物理屏幕人工冻结仍属于外部验收项，Linux/offscreen 结果不替代该签署。
