# MyGPR v0.9.0 beta 按钮与回调审计记录

## 审计范围

- 项目管理操作卡：导入测线、批量导入、导入 RTK/IMU、运行数据质检、新建项目、打开项目、项目设置、项目备份。
- 测线清单工具条：新建测线、导出清单、刷新。
- 质检详情与 B-scan 方向修正。
- 顶部当前项目切换。
- 批量导入结果对话框操作按钮。

## 本轮修复

- `项目备份` 不再是占位入口，已接入 `backup_project_archive()`。
- `导出清单` 不再是无回调按钮，已接入 `export_line_manifest_csv()`。
- 新增回归测试 `tests/test_beta_prep_v0900.py`，覆盖导出、备份、占位文案清理和 beta 文档存在性。

## 仍需本地确认

- Windows 双击启动后所有 QFileDialog 路径是否正常。
- 项目备份 ZIP 是否可在用户目录写入。
- 超大项目备份耗时是否可接受。
- 批量导入窗口在百万行 CSV 导入时是否保持可响应。
