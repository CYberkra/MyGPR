# MyGPR v0.8.74 代码健康快速发现

本文件是快速扫描结果，不代表所有位置都有问题。正式处理时应区分产品主线、历史兼容代码、CLI 脚本和研究脚本。

## 快速计数

- `except Exception`：约 379 处
- 裸 `except:`：0 处
- 独立 `pass`：约 109 处
- `print(...)`：约 340 处

## 初步判断

1. `except Exception` 很多集中在旧主窗口、兼容入口和脚本中，其中一部分是合理的 GUI 防御式兜底，但正式产品路径应逐步替换成明确日志和用户提示。
2. `print(...)` 很多来自 CLI、批处理、诊断脚本和研究验证脚本，不应一律删除；但 GUI 正式路径不应保留无意义调试打印。
3. 当前没有发现裸 `except:`，这是好现象，应继续保持。
4. `app_qt.py` 和 `ui/field_workbench_window.py` 是后续体积治理重点。

## 下一步处理顺序

### 优先处理

- GUI 正式路径中的 `except Exception: pass`
- 会吞掉项目保存、算法执行、结果导出错误的兜底
- 会导致用户看不到错误原因的日志缺口

### 暂缓处理

- CLI 脚本正常输出的 `print(...)`
- 历史研究脚本中的诊断输出
- 测试辅助代码中的简单兜底

## 建议规则

- 新增 GUI 代码不得使用裸 `except`。
- 新增正式路径的 `except Exception` 必须至少写日志或用户可见提示。
- 新增处理结果、目标标注、报告导出代码不得静默失败。
