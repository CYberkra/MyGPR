#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""GX-007 scene_001 material/target fix audit."""

# GX-007-FIX-001 Scene_001 Material/Target Fix Audit

Date: 2026-05-23  
Repo: `D:\CDUT-UavGPR-Controller\MyGPR`  
Branch: `main`

## Summary

- Fix target: `scene_001_single_shallow_pipe`
- Goal: remove `pipe_metal` non-physical propagation failure and keep pair contract.
- Result: **raw/background run succeeded** for scene_001.
- output_root used: `D:/CDUT-UavGPR-Controller/MyGPR-Evidence/gprmax/GX-007`
- target_response generated: **no** (not part of this task)

## Exact Material/Target Change

Files changed:

- `experiments/gprmax/GX-007/models/scene_001_single_shallow_pipe/raw_with_target.in`
- `experiments/gprmax/GX-007/models/scene_001_single_shallow_pipe/background_only.in`
- `experiments/gprmax/GX-007/models/scene_001_single_shallow_pipe/materials.txt`

Changes:

1. Removed extreme custom target material definition from `.in` files:
   - removed: `#material: 1000000.0 10000000.0 1.0 0.0 pipe_metal`
2. Kept background material only:
   - `#material: 9.0 0.01 1.0 0.0 background_soil`
3. Replaced raw target cylinder material from `pipe_metal` to built-in `pec`:
   - `#cylinder: ... pec`
4. Updated `materials.txt` note to reflect built-in PEC target usage for scene_001.

Why this fixes the failure:

- Previous error was triggered by the extreme `pipe_metal` constitutive parameters causing a non-physical wavelength sampling constraint (`wavelength sampled by 0 cells`).
- `pec` is a gprMax built-in conductor representation and avoids the unstable custom high-contrast material parameterization used previously.

## Pair Contract Check (scene_001)

raw/background remain consistent on:

- `#domain`
- `#dx_dy_dz`
- `#time_window`
- `#waveform`
- `#hertzian_dipole`
- `#rx`
- `#src_steps`
- `#rx_steps`
- `#material` background definition
- `#box` background region

Expected difference retained:

- raw contains target `#cylinder ... pec`
- background omits target object
- title and geometry_view basename differ

## Dry-run Result

Command:

```bash
python scripts/gprmax_campaign_runner.py --campaign experiments/gprmax/GX-007/campaign.yaml --dry-run
```

Result:

- campaign_status: `ready`
- total_scenes: `2`
- ready_count: `2`
- invalid_count: `0`

## Raw Run Result (scene_001, raw_with_target)

Command:

```bash
python scripts/gprmax_campaign_runner.py --campaign experiments/gprmax/GX-007/campaign.yaml --run-scene scene_001_single_shallow_pipe --variant raw_with_target --timeout-seconds 900
```

Result:

- status: `success`
- return_code: `0`
- runtime_seconds: `70.9758`
- manifest: `.../scene_001_single_shallow_pipe/raw_with_target/run_manifest.json`
- stderr: empty

## Background Run Result (scene_001, background_only)

Command:

```bash
python scripts/gprmax_campaign_runner.py --campaign experiments/gprmax/GX-007/campaign.yaml --run-scene scene_001_single_shallow_pipe --variant background_only --timeout-seconds 900
```

Result:

- status: `success`
- return_code: `0`
- runtime_seconds: `71.6774`
- manifest: `.../scene_001_single_shallow_pipe/background_only/run_manifest.json`
- stderr: empty

## Generated Output Check

- `.out/.vti` files were generated by gprMax during execution.
- No pairing artifacts (`target_response.npy/.csv`) were generated in this task.
- No target_response was generated.

## Validation

Command:

```bash
python scripts/preflight_check.py
```

Result:

- `[OK] Preflight passed`

## Repository Hygiene

- `.out/.h5/generated CSV` committed to MyGPR: **no**
- gprMax-generated files in MyGPR model folder were removed after verification.
- This commit includes only model/material text edits and this audit report.

## Claim Boundary

- Synthetic first-run fix only.
- Not field validation.
- Not AutoTune evaluation.
- Not paper-candidate result.

## Known Risks

1. Execution currently relied on a local command bridge to the user’s gprMax environment (outside repo baseline config).
2. This task validated scene_001 only; scene_002 still needs dedicated run verification.
3. Pairing/target_response pipeline is not executed in this task.

## Recommended Next Task

- `GX-007-PAIR-001`: run safe conversion/pairing on scene_001 outputs and generate target_response + preview/report artifacts.
