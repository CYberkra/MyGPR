campaign_id: GX-RUN-001_duplicate_scene
output_root: output/gprmax_campaign_duplicate_scene
gprmax_executable: gprMax
scenes:
  - scene_id: scene_dup
    raw_model: models/raw_with_target.in
    background_model: models/background_only.in
    materials: models/materials.txt
    target_roi: annotations/target_roi.json
    expected_outputs:
      - raw_with_target
      - background_only
      - target_response
    tags: [dup]
  - scene_id: scene_dup
    raw_model: models/raw_with_target.in
    background_model: models/background_only_2.in
    materials: models/materials.txt
    target_roi: annotations/target_roi.json
    expected_outputs:
      - raw_with_target
      - background_only
      - target_response
    tags: [dup]
